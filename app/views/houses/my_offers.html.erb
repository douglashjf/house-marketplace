<div class="container mt-5">
  <div>
    <%= link_to root_path, class: "btn" do %>
        <i class="fas fa-arrow-left"></i>
      <% end %>
  </div>
  <div class="text-center">
    <h1>My Offers</h1>
  </div>

  <div class="row justify-content-center mt-5">
    <% if @user_offers.present? %>
      <% @user_offers.each do |house| %>
        <div class="col-md-3 mb-4">
          <div class="card" style="width: 18rem;">
            <% if house.photo.attached? %>
              <%= image_tag(cl_image_path house.photo.key, width: 400, height: 300, crop: :fill) %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= link_to house.address, house_path(house) %></h5>
              <p class="card-text">Current Price: $<%= house.price %></p>
              <p class="card-text">Offered Price: $<%= house.offers.find_by(user: current_user).price %></p>
              <%= link_to "Click for more info", house_path(house), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="col-md-12 text-center">
        <div>
          <%= image_tag 'empty.webp', alt: "No Houses" %>
          <h3>There are no Offers yet.</h3>
        </div>
        <div>
          <%= link_to "View All Houses", houses_path, class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
